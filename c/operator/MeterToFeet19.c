#include<stdio.h>
#include<stdlib.h>
int main(int argc,char* argv[]){
    float length;
    printf("enter a length in meter");
    scanf("%f",&length);
    length *= 3.2803;
    printf("length in feet=%f",length);

     
}